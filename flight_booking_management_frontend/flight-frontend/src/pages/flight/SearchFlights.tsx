// Responsible for capturing user input (origin, destination, date)
// Will call FLIGHT.SEARCH API

const SearchFlights = () => {
  return (
    <div className="card">
      <h2>Search Flights</h2>
      <p>Loading... Page yet to be created</p>
    </div>
  );
};

export default SearchFlights;